# Copie para .env e escolha uma senha exclusiva (mínimo 12 caracteres).
ADMIN_PASSWORD=
APP_ORIGIN=http://localhost:3000
PORT=3000
# Em produção: NODE_ENV=production e APP_ORIGIN=https://seu-dominio
# DATA_DIR deve apontar para um disco persistente. Faça backup dessa pasta.
DATA_DIR=./data
